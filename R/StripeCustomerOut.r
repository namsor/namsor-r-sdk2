# NamSor API v2
#
# NamSor API v2 : enpoints to process personal names (gender, cultural origin or ethnicity) in all alphabets or languages. Use GET methods for small tests, but prefer POST methods for higher throughput (batch processing of up to 100 names at a time). Need something you can't find here? We have many more features coming soon. Let us know, we'll do our best to add it! 
#
# OpenAPI spec version: 2.0.5
# Contact: contact@namsor.com
# Generated by: https://openapi-generator.tech


#' StripeCustomerOut Class
#'
#' @field stripeCustomerId 
#' @field sourceCountry 
#' @field sourceCurrency 
#' @field stripedCards 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
StripeCustomerOut <- R6::R6Class(
  'StripeCustomerOut',
  public = list(
    `stripeCustomerId` = NULL,
    `sourceCountry` = NULL,
    `sourceCurrency` = NULL,
    `stripedCards` = NULL,
    initialize = function(`stripeCustomerId`, `sourceCountry`, `sourceCurrency`, `stripedCards`){
      if (!missing(`stripeCustomerId`)) {
        stopifnot(is.character(`stripeCustomerId`), length(`stripeCustomerId`) == 1)
        self$`stripeCustomerId` <- `stripeCustomerId`
      }
      if (!missing(`sourceCountry`)) {
        stopifnot(is.character(`sourceCountry`), length(`sourceCountry`) == 1)
        self$`sourceCountry` <- `sourceCountry`
      }
      if (!missing(`sourceCurrency`)) {
        stopifnot(is.character(`sourceCurrency`), length(`sourceCurrency`) == 1)
        self$`sourceCurrency` <- `sourceCurrency`
      }
      if (!missing(`stripedCards`)) {
        stopifnot(is.list(`stripedCards`), length(`stripedCards`) != 0)
        lapply(`stripedCards`, function(x) stopifnot(R6::is.R6(x)))
        self$`stripedCards` <- `stripedCards`
      }
    },
    toJSON = function() {
      StripeCustomerOutObject <- list()
      if (!is.null(self$`stripeCustomerId`)) {
        StripeCustomerOutObject[['stripeCustomerId']] <- self$`stripeCustomerId`
      }
      if (!is.null(self$`sourceCountry`)) {
        StripeCustomerOutObject[['sourceCountry']] <- self$`sourceCountry`
      }
      if (!is.null(self$`sourceCurrency`)) {
        StripeCustomerOutObject[['sourceCurrency']] <- self$`sourceCurrency`
      }
      if (!is.null(self$`stripedCards`)) {
        StripeCustomerOutObject[['stripedCards']] <- lapply(self$`stripedCards`, function(x) x$toJSON())
      }

      StripeCustomerOutObject
    },
    fromJSON = function(StripeCustomerOutJson) {
      StripeCustomerOutObject <- jsonlite::fromJSON(StripeCustomerOutJson)
      if (!is.null(StripeCustomerOutObject$`stripeCustomerId`)) {
        self$`stripeCustomerId` <- StripeCustomerOutObject$`stripeCustomerId`
      }
      if (!is.null(StripeCustomerOutObject$`sourceCountry`)) {
        self$`sourceCountry` <- StripeCustomerOutObject$`sourceCountry`
      }
      if (!is.null(StripeCustomerOutObject$`sourceCurrency`)) {
        self$`sourceCurrency` <- StripeCustomerOutObject$`sourceCurrency`
      }
      if (!is.null(StripeCustomerOutObject$`stripedCards`)) {
        self$`stripedCards` <- lapply(StripeCustomerOutObject$`stripedCards`, function(x) {
          stripedCardsObject <- StripeCardOut$new()
          stripedCardsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          stripedCardsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "stripeCustomerId": %s,
           "sourceCountry": %s,
           "sourceCurrency": %s,
           "stripedCards": [%s]
        }',
        self$`stripeCustomerId`,
        self$`sourceCountry`,
        self$`sourceCurrency`,
        lapply(self$`stripedCards`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(StripeCustomerOutJson) {
      StripeCustomerOutObject <- jsonlite::fromJSON(StripeCustomerOutJson)
      self$`stripeCustomerId` <- StripeCustomerOutObject$`stripeCustomerId`
      self$`sourceCountry` <- StripeCustomerOutObject$`sourceCountry`
      self$`sourceCurrency` <- StripeCustomerOutObject$`sourceCurrency`
      self$`stripedCards` <- lapply(StripeCustomerOutObject$`stripedCards`, function(x) StripeCardOut$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
