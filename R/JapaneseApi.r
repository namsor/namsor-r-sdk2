# NamSor API v2
#
# NamSor API v2 : enpoints to process personal names (gender, cultural origin or ethnicity) in all alphabets or languages. Use GET methods for small tests, but prefer POST methods for higher throughput (batch processing of up to 100 names at a time). Need something you can't find here? We have many more features coming soon. Let us know, we'll do our best to add it! 
#
# OpenAPI spec version: 2.0.10
# Contact: contact@namsor.com
# Generated by: https://openapi-generator.tech

#' @title Japanese operations
#' @description namsor.Japanese
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' gender_japanese_name_full Infer the likely gender of a Japanese full name ex. 王晓明
#'
#'
#' gender_japanese_name_full_batch Infer the likely gender of up to 100 full names
#'
#'
#' gender_japanese_name_pinyin Infer the likely gender of a Japanese name in LATIN (Pinyin).
#'
#'
#' gender_japanese_name_pinyin_batch Infer the likely gender of up to 100 Japanese names in LATIN (Pinyin).
#'
#'
#' japanese_name_kanji_candidates Identify japanese name candidates in KANJI, based on the romanized name ex. Yamamoto Sanae
#'
#'
#' japanese_name_kanji_candidates_batch Identify japanese name candidates in KANJI, based on the romanized name (firstName &#x3D; japaneseGivenName; lastName&#x3D;japaneseSurname), ex. Yamamoto Sanae
#'
#'
#' japanese_name_latin_candidates Romanize japanese name, based on the name in Kanji.
#'
#'
#' japanese_name_latin_candidates_batch Romanize japanese names, based on the name in KANJI
#'
#'
#' japanese_name_match Return a score for matching Japanese name in KANJI ex. 山本 早苗 with a romanized name ex. Yamamoto Sanae
#'
#'
#' japanese_name_match_batch Return a score for matching a list of Japanese names in KANJI ex. 山本 早苗 with romanized names ex. Yamamoto Sanae
#'
#'
#' japanese_name_match_feedback_loop [CREDITS 1 UNIT] Feedback loop to better perform matching Japanese name in KANJI ex. 山本 早苗 with a romanized name ex. Yamamoto Sanae
#'
#'
#' parse_japanese_name Infer the likely first/last name structure of a name, ex. 山本 早苗 or Yamamoto Sanae
#'
#'
#' parse_japanese_name_batch Infer the likely first/last name structure of a name, ex. 山本 早苗 or Yamamoto Sanae 
#'
#' }
#'
#' @export
JapaneseApi <- R6::R6Class(
  'JapaneseApi',
  public = list(
    userAgent = "OpenAPI-Generator/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    gender_japanese_name_full = function(japanese_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/genderJapaneseNameFull/{japaneseName}"
      if (!missing(`japanese_name`)) {
        urlPath <- gsub(paste0("\\{", "japaneseName", "\\}"), `japanese_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PersonalNameGenderedOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    gender_japanese_name_full_batch = function(batch_personal_name_in, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_personal_name_in`)) {
        body <- `batch_personal_name_in`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api2/json/genderJapaneseNameFullBatch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchPersonalNameGenderedOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    gender_japanese_name_pinyin = function(japanese_surname, japanese_given_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/genderJapaneseName/{japaneseSurname}/{japaneseGivenName}"
      if (!missing(`japanese_surname`)) {
        urlPath <- gsub(paste0("\\{", "japaneseSurname", "\\}"), `japanese_surname`, urlPath)
      }

      if (!missing(`japanese_given_name`)) {
        urlPath <- gsub(paste0("\\{", "japaneseGivenName", "\\}"), `japanese_given_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FirstLastNameGenderedOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    gender_japanese_name_pinyin_batch = function(batch_first_last_name_in, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_first_last_name_in`)) {
        body <- `batch_first_last_name_in`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api2/json/genderJapaneseNameBatch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchFirstLastNameGenderedOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_kanji_candidates = function(japanese_surname_latin, japanese_given_name_latin, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/japaneseNameKanjiCandidates/{japaneseSurnameLatin}/{japaneseGivenNameLatin}"
      if (!missing(`japanese_surname_latin`)) {
        urlPath <- gsub(paste0("\\{", "japaneseSurnameLatin", "\\}"), `japanese_surname_latin`, urlPath)
      }

      if (!missing(`japanese_given_name_latin`)) {
        urlPath <- gsub(paste0("\\{", "japaneseGivenNameLatin", "\\}"), `japanese_given_name_latin`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RomanizedNameOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_kanji_candidates_batch = function(batch_first_last_name_in, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_first_last_name_in`)) {
        body <- `batch_first_last_name_in`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api2/json/japaneseNameKanjiCandidatesBatch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchNameMatchCandidatesOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_latin_candidates = function(japanese_surname_kanji, japanese_given_name_kanji, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/japaneseNameLatinCandidates/{japaneseSurnameKanji}/{japaneseGivenNameKanji}"
      if (!missing(`japanese_surname_kanji`)) {
        urlPath <- gsub(paste0("\\{", "japaneseSurnameKanji", "\\}"), `japanese_surname_kanji`, urlPath)
      }

      if (!missing(`japanese_given_name_kanji`)) {
        urlPath <- gsub(paste0("\\{", "japaneseGivenNameKanji", "\\}"), `japanese_given_name_kanji`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RomanizedNameOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_latin_candidates_batch = function(batch_first_last_name_in, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_first_last_name_in`)) {
        body <- `batch_first_last_name_in`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api2/json/japaneseNameLatinCandidatesBatch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchNameMatchCandidatesOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_match = function(japanese_surname_latin, japanese_given_name_latin, japanese_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/japaneseNameMatch/{japaneseSurnameLatin}/{japaneseGivenNameLatin}/{japaneseName}"
      if (!missing(`japanese_surname_latin`)) {
        urlPath <- gsub(paste0("\\{", "japaneseSurnameLatin", "\\}"), `japanese_surname_latin`, urlPath)
      }

      if (!missing(`japanese_given_name_latin`)) {
        urlPath <- gsub(paste0("\\{", "japaneseGivenNameLatin", "\\}"), `japanese_given_name_latin`, urlPath)
      }

      if (!missing(`japanese_name`)) {
        urlPath <- gsub(paste0("\\{", "japaneseName", "\\}"), `japanese_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RomanizedNameOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_match_batch = function(batch_first_last_name_in, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_first_last_name_in`)) {
        body <- `batch_first_last_name_in`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api2/json/japaneseNameMatchBatch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchNameMatchCandidatesOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    japanese_name_match_feedback_loop = function(japanese_surname_latin, japanese_given_name_latin, japanese_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/japaneseNameMatchFeedbackLoop/{japaneseSurnameLatin}/{japaneseGivenNameLatin}/{japaneseName}"
      if (!missing(`japanese_surname_latin`)) {
        urlPath <- gsub(paste0("\\{", "japaneseSurnameLatin", "\\}"), `japanese_surname_latin`, urlPath)
      }

      if (!missing(`japanese_given_name_latin`)) {
        urlPath <- gsub(paste0("\\{", "japaneseGivenNameLatin", "\\}"), `japanese_given_name_latin`, urlPath)
      }

      if (!missing(`japanese_name`)) {
        urlPath <- gsub(paste0("\\{", "japaneseName", "\\}"), `japanese_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RomanizedNameOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    parse_japanese_name = function(japanese_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api2/json/parseJapaneseName/{japaneseName}"
      if (!missing(`japanese_name`)) {
        urlPath <- gsub(paste0("\\{", "japaneseName", "\\}"), `japanese_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- PersonalNameParsedOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    parse_japanese_name_batch = function(batch_personal_name_in, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_personal_name_in`)) {
        body <- `batch_personal_name_in`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api2/json/parseJapaneseNameBatch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchPersonalNameParsedOut$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
