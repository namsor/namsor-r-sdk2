# NamSor API v2
#
# NamSor API v2 : enpoints to process personal names (gender, cultural origin or ethnicity) in all alphabets or languages. Use GET methods for small tests, but prefer POST methods for higher throughput (batch processing of up to 100 names at a time). Need something you can't find here? We have many more features coming soon. Let us know, we'll do our best to add it! 
#
# The version of the OpenAPI document: 2.0.5
# Contact: contact@namsor.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BillingHistoryOut
#' @description BillingHistoryOut Class
#' @format An \code{R6Class} generator object
#' @field stripeInvoices   \link[namsor:InvoiceOut]{   list(InvoiceOut)   }  [optional]
#'
#' @field corporateInvoices   \link[namsor:InvoiceOut]{   list(InvoiceOut)   }  [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BillingHistoryOut <- R6::R6Class(
  'BillingHistoryOut',
  public = list(
    `stripeInvoices` = NULL,
    `corporateInvoices` = NULL,
    initialize = function(`stripeInvoices`=NULL, `corporateInvoices`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`stripeInvoices`)) {
        stopifnot(is.vector(`stripeInvoices`), length(`stripeInvoices`) != 0)
        sapply(`stripeInvoices`, function(x) stopifnot(R6::is.R6(x)))
        self$`stripeInvoices` <- `stripeInvoices`
      }
      if (!is.null(`corporateInvoices`)) {
        stopifnot(is.vector(`corporateInvoices`), length(`corporateInvoices`) != 0)
        sapply(`corporateInvoices`, function(x) stopifnot(R6::is.R6(x)))
        self$`corporateInvoices` <- `corporateInvoices`
      }
    },
    toJSON = function() {
      BillingHistoryOutObject <- list()
      if (!is.null(self$`stripeInvoices`)) {
        BillingHistoryOutObject[['stripeInvoices']] <-
          lapply(self$`stripeInvoices`, function(x) x$toJSON())
      }
      if (!is.null(self$`corporateInvoices`)) {
        BillingHistoryOutObject[['corporateInvoices']] <-
          lapply(self$`corporateInvoices`, function(x) x$toJSON())
      }

      BillingHistoryOutObject
    },
    fromJSON = function(BillingHistoryOutJson) {
      BillingHistoryOutObject <- jsonlite::fromJSON(BillingHistoryOutJson)
      if (!is.null(BillingHistoryOutObject$`stripeInvoices`)) {
        self$`stripeInvoices` <- ApiClient$new()$deserializeObj(BillingHistoryOutObject$`stripeInvoices`, "array[InvoiceOut]", "package:namsor")
      }
      if (!is.null(BillingHistoryOutObject$`corporateInvoices`)) {
        self$`corporateInvoices` <- ApiClient$new()$deserializeObj(BillingHistoryOutObject$`corporateInvoices`, "array[InvoiceOut]", "package:namsor")
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`stripeInvoices`)) {
        sprintf(
        '"stripeInvoices":
        [%s]
',
        paste(sapply(self$`stripeInvoices`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`corporateInvoices`)) {
        sprintf(
        '"corporateInvoices":
        [%s]
',
        paste(sapply(self$`corporateInvoices`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(BillingHistoryOutJson) {
      BillingHistoryOutObject <- jsonlite::fromJSON(BillingHistoryOutJson)
      self$`stripeInvoices` <- ApiClient$new()$deserializeObj(BillingHistoryOutObject$`stripeInvoices`, "array[InvoiceOut]","package:namsor")
      self$`corporateInvoices` <- ApiClient$new()$deserializeObj(BillingHistoryOutObject$`corporateInvoices`, "array[InvoiceOut]","package:namsor")
      self
    }
  )
)
