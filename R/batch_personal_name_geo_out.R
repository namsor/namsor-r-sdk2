# NamSor API v2
#
# NamSor API v2 : enpoints to process personal names (gender, cultural origin or ethnicity) in all alphabets or languages. Use GET methods for small tests, but prefer POST methods for higher throughput (batch processing of up to 100 names at a time). Need something you can't find here? We have many more features coming soon. Let us know, we'll do our best to add it! 
#
# The version of the OpenAPI document: 2.0.10
# Contact: contact@namsor.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title BatchPersonalNameGeoOut
#' @description BatchPersonalNameGeoOut Class
#' @format An \code{R6Class} generator object
#' @field personalNames  list( \link{PersonalNameGeoOut} ) [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
BatchPersonalNameGeoOut <- R6::R6Class(
  'BatchPersonalNameGeoOut',
  public = list(
    `personalNames` = NULL,
    initialize = function(`personalNames`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`personalNames`)) {
        stopifnot(is.vector(`personalNames`), length(`personalNames`) != 0)
        sapply(`personalNames`, function(x) stopifnot(R6::is.R6(x)))
        self$`personalNames` <- `personalNames`
      }
    },
    toJSON = function() {
      BatchPersonalNameGeoOutObject <- list()
      if (!is.null(self$`personalNames`)) {
        BatchPersonalNameGeoOutObject[['personalNames']] <-
          lapply(self$`personalNames`, function(x) x$toJSON())
      }

      BatchPersonalNameGeoOutObject
    },
    fromJSON = function(BatchPersonalNameGeoOutJson) {
      BatchPersonalNameGeoOutObject <- jsonlite::fromJSON(BatchPersonalNameGeoOutJson)
      if (!is.null(BatchPersonalNameGeoOutObject$`personalNames`)) {
        self$`personalNames` <- ApiClient$new()$deserializeObj(BatchPersonalNameGeoOutObject$`personalNames`, "array[PersonalNameGeoOut]", loadNamespace("namsor"))
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`personalNames`)) {
        sprintf(
        '"personalNames":
        [%s]
',
        paste(sapply(self$`personalNames`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(BatchPersonalNameGeoOutJson) {
      BatchPersonalNameGeoOutObject <- jsonlite::fromJSON(BatchPersonalNameGeoOutJson)
      self$`personalNames` <- ApiClient$new()$deserializeObj(BatchPersonalNameGeoOutObject$`personalNames`, "array[PersonalNameGeoOut]", loadNamespace("namsor"))
      self
    }
  )
)
